services:
  - type: web
    name: relay-superagent
    runtime: python
    plan: free
    buildCommand: "pip install -e ."
    startCommand: "python3 demo/server.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
